machine:
    services:
        - docker

dependencies:
    override:
        - sudo pip install -U docker-compose==1.4.1

test:
    override:
        - docker-compose run -d --no-deps node1
        - cd node; mocha